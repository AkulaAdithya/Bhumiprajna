Investigate why every project is receiving exactly 66% delay probability. Do not retrain or regenerate the model yet. Trace the complete prediction path from the saved project/snapshot data → feature extraction/feature engineering → model input → model.predict/predict_proba → probability transformation → API response → frontend display. Compare two substantially different projects and print/log their actual model feature vectors and raw prediction probabilities. Check for hardcoded/default/fallback 66% values, failed feature mapping, missing features becoming identical defaults, incorrect model loading, or probability transformation bugs. Verify that different project inputs produce different model inputs and raw predictions. Fix the root cause only, then test at least 3 projects with substantially different risk profiles and confirm their raw and displayed probabilities differ appropriately. Do not make unrelated changes.
Run relevant backend tests and frontend build/type checks. Do not make unrelated changes.